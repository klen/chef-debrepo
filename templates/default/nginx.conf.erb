server {

    listen 80;

    <%= if node[:debrepo][:host] %>
    server_name <%= node[:debrepo][:host] %>;
    <%= end %>

    location /<%= node[:debrepo][:name] %> {
        root <%= node[:debrepo][:source_dir] %>;
        autoindex on;
    }

}
